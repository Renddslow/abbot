import styled from 'styled-components';

const Button = styled.button`
  font-size: inherit;
  font-family: inherit;
  color: #75767c;
  background: #eff0fa;
  border: 0;
  border-radius: 4px;
  padding: 8px 12px;
  font-weight: 600;
  cursor: pointer;
`;

export default Button;
